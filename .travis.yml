language: go

before_install:
  - gvm install go1.1 && gvm use go1.1
  - go version
  - sudo apt-get install libzmq3-dev

install:
  - go get -tags zmq_3_x -d -v

script:
  - go test -tags zmq_3_x -v
